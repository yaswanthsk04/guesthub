apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: prometheus:9090
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: loki:3100
    jsonData:
      maxLines: 1000
